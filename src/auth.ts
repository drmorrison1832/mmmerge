/**
 * Flux OAuth2 Google (Desktop app). Voir architecture.md §2. STUB — à implémenter.
 */
export async function authenticate(): Promise<unknown> {
  throw new Error('authenticate: not implemented');
}
