/**
 * Exécute les 3 phases dans l'ordre, pour chaque ligne éligible. Voir architecture.md §3.
 * STUB — à implémenter.
 */
import type { Config } from '../config/schema.js';

export async function runPipeline(_profile: Config, _cliFlags: Record<string, unknown>): Promise<number> {
  throw new Error('runPipeline: not implemented');
}
